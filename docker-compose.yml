services:
  bot:
    build: .
    image: themonitorbot:latest
    container_name: themonitorbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/data
    stop_grace_period: 30s
    init: true

volumes:
  bot-data:
